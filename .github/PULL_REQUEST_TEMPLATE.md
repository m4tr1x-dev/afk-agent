## What this changes

<!-- One or two sentences. What is different after this merges? -->

## Why

<!-- Link the issue, or state the reasoning. If this implements a specification
     page, link the page. If it contradicts one, say so explicitly — that is a
     legitimate thing to do, but it must be visible. -->

Closes #

## Requirement identifiers

<!-- List the FR-/NFR-/INV- identifiers this touches, or write "none" and say
     why. Code that implements a requirement carries its identifier in a
     comment, and its test carries it in an annotation. -->

## Documentation

- [ ] Updated the matching specification or reference page
- [ ] Not needed, because: <!-- a real reason, not "trivial change" -->

<!-- Generated reference pages are regenerated by CI and compared against what
     is committed. If that check fails, run the generator and commit the
     result; do not edit a generated page by hand. -->

## Safety

<!-- Delete this section only if the change cannot possibly touch input,
     capture, or the safety interlocks. -->

- [ ] No new call site for input injection outside the executor
- [ ] Held input is still released on stop, panic, focus loss and crash
- [ ] No learned or model-driven component can relax a safety constraint

## Verification

<!-- What did you actually run? "CI passed" is not an answer on its own for a
     behavioural change. Name the test, the manual scenario, or the measurement,
     and paste the number if there is one. -->

## Anything a reviewer should look at twice

<!-- Optional. The part you are least sure about. -->
